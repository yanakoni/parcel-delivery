const MAX_IMAGE_SIZE_BYTES = 10_000_000;
const MAX_DOC_IMAGE_SIZE_BYTES = 120_000_000;

const phoneRegex = /^([+]?[\s0-9]+)?(\d{3}|[(]?[0-9]+[)])?([-]?[\s]?[0-9])+$/;

export { MAX_IMAGE_SIZE_BYTES, MAX_DOC_IMAGE_SIZE_BYTES, phoneRegex };
